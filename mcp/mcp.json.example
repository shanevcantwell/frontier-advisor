{
  "mcpServers": {
    "frontier-advisor": {
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "-v", "${HOME}/.claude:/home/advisor/.claude:ro",
        "mcp/frontier-advisor"
      ]
    }
  }
}
